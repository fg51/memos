# run

```sh
$ pnpm dlx @azure/static-web-apps-cli --version
```

## isntall

```sh
$ pnpm install --dev @azure/static-web-apps-cli
$ swa --version
```

# run local

swa

```sh
$ swa start https://localhost:8080 --run "pnpm start"
```

```sh
$ pnpx @azure/static-web-apps-cli start ./dist --port 8080
```
